/**
 * Package types enumeration.
 * Defines the different types of packages available.
 */
export enum PackageTypeEnum {
  FREE = "free",
  ALPHA = "alpha",
  BASIC = "basic",
  PREMIUM = "premium"
}